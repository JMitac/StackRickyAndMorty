name: StackRickAndMortin

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
        run: echo "deploy application"
      
      - name: Install 🔧
        run: yarn install
      
      - name: Build 🏗️
        run: yarn build
